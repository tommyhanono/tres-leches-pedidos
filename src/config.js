// ─────────────────────────────────────────────────────────────
//  CONFIGURACIÓN — edita estos valores cuando quieras
// ─────────────────────────────────────────────────────────────

// Precio de venta por unidad del postre (USD).
export const PRECIO_UNITARIO = 9.0;

// Costo de producción por unidad (USD). Sirve para calcular la ganancia.
export const COSTO_UNITARIO = 1.8;

// Datos del producto y del pago (se muestran en la app).
export const PRODUCTO = "Tres Leches casero";
export const YAPPY_NOMBRE = "Liel";
export const YAPPY_NUMERO = "+507 6982-4595";

// Subtítulo de la causa.
export const CAUSA = "Las ventas ayudan a una persona que necesita ayuda auditiva 💙";

// Sensibilidad del detector de comprobantes duplicados (distancia de Hamming
// sobre un hash de 64 bits). Más bajo = más estricto. 10 es un buen punto medio.
export const UMBRAL_DUPLICADO = 10;
